import React from 'react';
import { Styles } from '../styles';
const { Common } = Styles;

const AppModal = ({}) => {
  return (
    <Common.Links.Normal to="#">Hello</Common.Links.Normal>
  )
}

export default AppModal;
